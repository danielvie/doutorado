\section{Software description}

This chapter will elaborate on the software architecture. It is composed of a two-parts: a high-precision ESP32-based signal generator
and a web-based remote control panel. This integration enables real-time configuration, control, and monitoring of the hardware
directly from a web browser via Bluetooth Low Energy (BLE). We will deconstruct the architecture of both the ESP32 firmware
and the React web application.

\subsection{System Architecture}

\begin{enumerate}
    \item \textbf{The Embedded System (ESP32 Firmware)}: A C++ application running on an ESP32 microcontroller. Its sole
          purpose is to generate precise digital signals on its GPIO pins and report back sensor data. It acts as a Bluetooth
          GATT server, waiting for commands and sending out data.
    \item \textbf{The Web Control Interface (React App)}: A web application built with React and TypeScript. It serves as
          the user's command center to define the signal patterns, send commands to the ESP32 and visualize incoming sensor
          data on a live chart. It acts as a Bluetooth GATT client, connecting to and controlling the ESP32.
\end{enumerate}

The bridge between these two parts is the Web Bluetooth API, which allows the browser-based application to communicate
wirelessly and directly with the ESP32 hardware.

\subsection{Part 1: The Embedded System (ESP32 Firmware)}
The firmware is built to be a robust, real-time signal generation engine leveraging features of the ESP32
and the FreeRTOS operating system to achieve high performance and reliability.

\subsubsection{FreeRTOS: The Real-Time Engine}
The ESP32 SDK is build on top of FreeRTOS, a real-time operating system designed for embedded devices.
A RTOS guarantees that tasks can be executed with precise timing and this project uses FreeRTOS for:

\begin{itemize}
    \item \textbf{Multitasking:} The firmware runs two primary tasks concurrently: `signalTask` and `bleTask`. The `bleTask'
          handles all the non-time-critical Bluetooth communication, while the `signalTask' focuses on managing the signal
          state machine
    \item \textbf{Task Pinning:} The ESP32 has a dual-core processor. To prevent the BLE stack from interfering with signal
          timing, both tasks are "pinned" to a specific CPU core using `xTaskCreatePinnedToCore' This prevents that a spike in
          Bluetooth activity on Core 1 will not delay the signal generation logic running alongside it.
    \item \textbf{Priority Management:} The `signalTask' is assigned a high priority, ensuring it gets processor time
          when it needs, to enforce the real-time guarantees.
\end{itemize}

Here is how the tasks are created and pinned to CORE\_1 and CORE\_0

\begin{lstlisting}[caption={Example of FreeRTOS Task Creation}]
// from main.cpp

// Create BLE task on Core 1 with high stack size for BLE operations
xTaskCreatePinnedToCore(
    bleTask,                    // Task function
    "BLE Task",                 // Task name
    8192,                       // Stack size (bytes)
    NULL,                       // Task parameter
    2,                          // Priority
    NULL,                       // Task handle
    CORE_1                      // CPU core 1
);

// Create signal task on Core 1 with high priority for precise timing
xTaskCreatePinnedToCore(
    signalTask,                 // Task function
    "Signal Task",              // Task name
    2048,                       // Stack size (bytes)
    NULL,                       // Task parameter
    configMAX_PRIORITIES - 2,   // High priority (second highest)
    NULL,                       // Task handle
    CORE_0                      // CPU core 0
);
\end{lstlisting}

\subsubsection{ESP32 Hardware Timers}
Software delays like `delay()' are not capable of generating signals with microsecond accuracy.
So, this system uses one of the ESP32's built-in hardware timers.

\begin{itemize}
    \item \textbf{How it Works:} The timer is a dedicated hardware that counts at a very high frequency.
          It is configure with a prescaler (TIMER\_DIVIDER), which divides the CPU's main clock (240MHz) to create
          a slower, more manageable timer clock (3MHz in this case).
    \item \textbf{Interrupt Service Routine (ISR):} The timer is set to trigger a hardware interrupt when its counter reaches
          a "alarm" value. This interrupt executes a function (Interrupt Service Routine). In this project, the `timer\_group\_isr\_callback'
          is the ISR. This function contains the code needed to switch the GPIO pins to their next state and set the alarm for the next
          interrupt.
    \item \textbf{IRAM Execution:} For maximum speed and to avoid delays from flash memory access, the ISR is loaded into the ESP32's
          Instruction RAM (IRAM) using the IRAM\_ATTR attribute.
\end{itemize}


\begin{lstlisting}[caption={Core ISR logic}]
// from main.cpp

bool IRAM_ATTR timer_group_isr_callback(void *args) {
    // Re-enable timer alarm for next interrupt
    timer_group_enable_alarm_in_isr(TIMER_GROUP, TIMER_IDX);

    // Only execute signal generation when in SIGNAL_RUN state
    if (g_signal_task_state != SignalTaskState::SIGNAL_RUN) {
        return true;
    }

    // Pre-calculate next state
    uint8_t next_state = (g_current_state + 1) % g_active_num_timings;
    uint32_t new_gpio_out_value = 0;

    // Get reference to active dataset
    DataSet& dataset = (g_active_set == SignalSet::SET_A) ? 
                        g_dataset_a : g_dataset_b;

    // Build pin state mask based on active signal pattern
    new_gpio_out_value = (dataset.d4_vec[next_state] ? GPIO_DI4_MASK : 0) |
                         (dataset.d5_vec[next_state] ? GPIO_DI5_MASK : 0) |
                         (dataset.d6_vec[next_state] ? GPIO_DI6_MASK : 0);

    // Write directly to the GPIO_OUT_REG for simultaneous update
    GPIO.out = (GPIO.out & ~GPIO_PIN_MASK) | new_gpio_out_value;

    // Update state
    g_current_state = next_state;
    timer_set_alarm_value(TIMER_GROUP, TIMER_IDX, 
                          dataset.time_vec[g_current_state]);
    
    // Check for cycle completion and trigger analog reading
    if (g_current_state == 0) {
        g_cycle_count = (g_cycle_count + 1) % g_cycle_nrun;
        if (g_cycle_count == 0) {
            g_ble_task_state = BLETaskState::ANALOG_READ;
        }
    }

    return true;
}
\end{lstlisting}

\subsubsection{Blutooth Low Energy (BLE)}
Bluetooth Low Energy is a wireless communication protocol designed for sending small amounts of data with very low power
consumption. It operates on a server-client model known as GATT (Generic Attribute Profile).

\begin{itemize}
    \item \textbf{GAT Server (The ESP32):} The ESP32 is configured as a GAT Server. It creates and advertises a Service
          (identified by SERVICE\_UUID).
    \item \textbf{Characteristics (The Data Points):} Inside the service, it defines a Characteristic (identified by
          CHARACTERISTIC\_UUID). This is the actual endpoint for communication. The firmware configures this characteristic to
          be writable and to support notifications (to send data back to the client).
    \item \textbf{NimBLE Library:} The project uses the NimBLE-Arduino library, a highly efficient and memory-optimized
          BLE stack for the ESP32.
\end{itemize}

The BLE service and characteristic are defined and created in main.cpp


\begin{lstlisting}[caption={BLE service}]
// from main.cpp

// BLE Service and Characteristic UUIDs for remote control
#define SERVICE_UUID "4fafc201-1fb5-459e-8fcc-c5c9c331914b"
#define CHARACTERISTIC_UUID "beb5483e-36e1-4688-b7f5-ea07361b26a8"

// ... inside bleTask() ...
NimBLEDevice::init("ESP32 Signal Controller");
NimBLEServer *pServer = NimBLEDevice::createServer();
// ...
NimBLEService *pService = pServer->createService(SERVICE_UUID);
NimBLECharacteristic *pCharacteristic = pService->createCharacteristic(
    CHARACTERISTIC_UUID,
    NIMBLE_PROPERTY::READ | NIMBLE_PROPERTY::WRITE | NIMBLE_PROPERTY::NOTIFY
);
pCharacteristic->setCallbacks(new CharacteristicCallbacks());
}
\end{lstlisting}

\subsection{Part 2: The Web Control Interface}

This is the user interface of the system for controlling the ESP32.
It is build with web technologies to run in any browser that supports
Web Bluetooth (like Google Chrome)

\subsubsection{Core Capabilities}

\begin{itemize}
    \item \textbf{React \& TypeScript:} The application is build using React, a popular
          JavaScript library for building user interfaces, and TypeScript, which adds static
          typing to JavaScript. This combination allow a code that is both portable and
          maintainable.
    \item \textbf{Web Bluetooth API:} The Web Bluetooth API is a browser standard that
          exposes Bluetooth functionality to JavaScript, allowing the application to
          \subitem \textbf{Scan} for nearby BLE devices
          \subitem \textbf{Connect} to a selected device (ESP32)
          \subitem \textbf{Discover} its services and characteristics
          \subitem \textbf{Write} data to characteristics (to send commands)
          \subitem \textbf{Subscribe} to notifications from characteristic (to receive sensor data)
\end{itemize}

This eliminates the need for any native software, drivers, or browser plugins. The entire control
system runs directly in the browser.


\begin{lstlisting}[caption={Bluetooth connect\_device}]
// from bluetooth.ts

export async function connect_device() {
    try {
        let navigatorObject: any = window.navigator;

        // Open the device picker dialog, filtering for our specific service
        g_bluetoothDevice = await navigatorObject.bluetooth.requestDevice({
            filters: [{ services: [SERVICE_UUID] }],
        });

        // Connect to the device's GATT server
        g_gattServer = await g_bluetoothDevice.gatt.connect();

        // Get the service we're interested in
        const service = await g_gattServer.getPrimaryService(SERVICE_UUID);

        // Get the characteristic we'll use for reading/writing data
        g_characteristic = await service.getCharacteristic(CHARACTERISTIC_UUID);

    } catch (error: any) {
        // Handle any errors during the connection process
    }
}
\end{lstlisting}

\subsection{Web Interface}

The web interface is built with React.js to provide an easy user experience.
This interface serves as the primary connection point between the user and the hardware,
linking directly to the ESP32 microcontroller via Bluetooth Low Energy (BLE).

From the web application (Figure \ref{fig:sw_web_interface}), users can easily send string commands to the device, as well as trigger automated
sequences for common experimental procedures. A key function of the interface is its ability to perform
on-the-fly computations to generate the control signals needed for the power switches. It also
receives and processes data from the ESP32's BLE connection, specifically the analog measurements from
the circuit. The received data is then visually represented in a real-time chart, allowing for immediate
analysis of the circuit's performance.


\begin{figure}[h!]
    \centering
    \includegraphics[width=140mm]{images/hw_esp32_pins.pdf}
    \caption{ESP32 Digital and Analog mapping}
    \label{fig:sw_esp_pins}
\end{figure}


\begin{figure}[h!]
    \centering
    \includegraphics[width=140mm]{images/sw_web_interface.pdf}
    \caption{Web Interface to connect and control the ESP32.}
    \label{fig:sw_web_interface}
\end{figure}

The web interface provides single-page application for interacting
with the hardware in real-time. It connects to the ESP32 via Bluetooth Low Energy (BLE) and serves as
a command center for controlling the circuit and analyzing its performance. The following are the key
features of the interface:

\begin{itemize}
    \item \textbf{1. Connect/Disconnect:} Establish or terminate a BLE connection to the ESP32.
    \item \textbf{2. Enter command:} Allows sending custom command to ESP32 through BLE.
    \item \textbf{3. SEND command:} Sends the custom command.
    \item \textbf{4. Alpha:} Input tor selecting the alpha value to compute the signal.
    \item \textbf{5. Time:} Time vector in micro seconds ($\mu s$)

        each element is a amount of $\mu s$ that the mode is going to be executed.

    \item \textbf{6. Mode:} Mode vector for the digital output
        
        the mode is the decimal representation of the three digital outputs.
        The digital output is set to send value to D4, D5 and D6 (figure \ref{fig:sw_esp_pins}). 

        When 6.1 is clicked then the interface shows the decomposed values for each channel D4, D5 and D6.

        \begin{figure}[h!]
            \centering
            \includegraphics[width=90mm]{images/sw_web_interface_modes_bin.png}
            \caption{Binary command values for D4, D5, D6 signal}
            \label{fig:sw_web_interface_modes_bin}
        \end{figure}
        
        The example in figure \ref{fig:sw_web_interface}, generates the digital signals
        d4: [0, 0, 0, 0, 1, 0], d5: [0, 0, 1, 0, 0, 0], d6: [1, 0, 0, 0, 0, 0] as seen 
        in figure \ref{fig:sw_web_interface_modes_bin}.

        represented in Figure \ref{fig:sw_signal}.
        
\begin{figure}[h!]
    \centering
    \includegraphics[scale=1.4]{images/sw_signal.pdf}
    \caption{Signal for $\alpha$ of 0.3}
    \label{fig:sw_signal}
\end{figure}

    \item \textbf{7. SEND:} Sends the signal to the ESP32.
    \item \textbf{8. START SIGNAL:} Commands the ESP32 to start reproducing the signal created.
    \item \textbf{9. LOW:} Commands the ESP32 to set all digital outputs to LOW (0).
    \item \textbf{10. HIGH:} Commands the ESP32 to set all digital outputs to HIGH (1).
    \item \textbf{11. chart toggle:} Collapses or expand the Chart to show the voltage reading via BLE.
    \item \textbf{12. advanced menu:} Collapses or expand the menu with pre-configured facility commands to send messages via BLE.


    \item \textbf{13. Monitor Chart:} When `Start Listening` is active, then the Monitor chart shows the values of the analog readings from the ESP32.

    \item \textbf{14. Start Listening:} Allow the web-interface to show the messages sent by ESP32 via BLE.

        the values are status messages and analog data that populates the Monitor Chart

    \item \textbf{15. copy:} Copy the data populating the monitor charts to the clipboard.
    \item \textbf{16. clear data:} Clear the data populating the monitor charts.
    \item \textbf{17. $<img>$:} When clicked on the image, the web interface then shows the schematics of the circuit and the connections of the 
        ESP32. This is used as a reference while connecting the circuit.


\end{itemize}

The system's behavior is described by a state diagram in Figure \ref{fig:state_diagram} that shows the
Overall System Flow, which transitions from an initial Setup phase into two main concurrent processes: BLE Task States and Signal Task States.

The BLE (Bluetooth Low Energy) task handles wireless communication
and sensor data, cycling through states such as BLE\_IDLE (when no operations are active), ANALOG\_READING
(when it's actively reading sensor data), and SIGNAL\_READING (when it's processing new signal information).

Concurrently, the Signal task manages the output signals, operating in states like IDLE (with no signals
generated), HIGH\_ALL (where all outputs are forced high), and SIGNAL\_RUN (for active signal pattern
generation). This dual-task structure allows the system to manage sensor inputs and signal outputs independently.

\begin{figure}[h]
    \centering
    \includegraphics[scale=0.5]{images/sw_state_machine.png}
    \caption{State Diagram of the SW Operation Modes}
    \label{fig:state_diagram}
\end{figure}
