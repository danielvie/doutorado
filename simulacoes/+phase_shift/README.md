Run "problemConfiguration.m" to create the "problemConfig" data
structure and to obtain the mode sequence "Omega" and the intervals
between switching times "dt" associated with the phase-shifted modulation scheme.

Note that the "dt" elements are all the same in the phase-shifted modulation scheme.

The configuration is currently set to that used by Benmiloud et al. (2016).